{
  "private": true,
  "name": "imagizer-cloudflare-worker",
  "version": "1.0.0",
  "description": "A Cloudflare worker to Integrate Imagizer without modifying the website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Nicholas Pettas <nick@nventify.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
